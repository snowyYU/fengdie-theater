Object(投资大剧场) {
  btnImg(播放按钮的图片): Image,
  startImg(播放按钮播放中): Image,
  stopImg(播放按钮暂停): Image,
  titleMain(主标题): Image,
  titleVice(副标题): Image,
  bgImg(背景图片): Image,
  heyMan(一位拉小提琴的男纸): Image,
  btnCenter(正中按钮): Image,
  btnCenteRipper(正中按钮波纹): Image,
  flower(花): Image,
  slider(下拉): Image,
  btnRight(右侧按钮): Image,
  presentBgImg(献花弹出框背景): Image,
  oKnowBtn(知道了按钮): Image,
  presentCloseBtn(献花的关闭按钮): Image,
  goToSee(去看看按钮): Image
}
